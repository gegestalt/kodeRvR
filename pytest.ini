[pytest]
# Make the source-layout package importable without installation.
pythonpath = src
testpaths = tests
addopts = -q
markers =
    live_data: opt-in tests that access authoritative upstream sources
    external_fixture: tests using genuine or cached external fixtures
    evaluation: offline evaluation against pinned verified fixtures
    slow: tests unsuitable for the fast default suite
